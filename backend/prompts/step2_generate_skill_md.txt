你是银行信贷知识工程师。请根据以下场景骨架和知识文档，生成一份完整的 Agent SKILL.md 文档。

## 场景骨架
场景名称：{{scenario_name}}
场景说明：{{scenario_desc}}
子场景列表：
{{sub_scenarios}}

模板知识列定义：
{{knowledge_columns}}

## 知识来源文档
{{source_text}}

## 输出要求

生成一份完整的 SKILL.md，遵循以下结构：

```markdown
# {{scenario_name}}

## 执行说明
本 Skill 用于 [场景描述]，按 [客户筛选 → 客户数据匹配 → 原因归因 → 决策建议] 四个阶段逐步执行。

## 知识规则

### 阶段一：客户筛选
[从知识文档中提取的客户筛选规则，每条规则包含：]
- **规则 1**：[规则名称]
  - 业务描述：[一句话说明这条规则做什么]
  - 数据来源：[数据从哪里来，如 CRM、核心系统、征信报告等]
  - 规则逻辑：[具体的筛选条件，尽量用业务语言表述清晰]
  - SQL 参考：
    ```sql
    SELECT [字段列表] FROM [表名] WHERE [条件]
    ```
  - 输出：[这步筛选后得到什么]

[每个阶段放 2-4 条规则，必须覆盖知识文档中提到的所有关键筛选条件]

### 阶段二：客户数据匹配
[同上结构，规则内容聚焦在如何匹配客户的多维度数据]

### 阶段三：原因归因
[同上结构，规则内容聚焦在如何判断客户属于哪种情况（有意/无意、符合/不符合条件等）]

### 阶段四：决策建议
[同上结构，规则内容聚焦在针对不同归因结果给出具体的营销/审批/培育建议]

## 附录：术语表
[提取文档中的专业术语并解释]
```

## 重要要求
1. SQL 必须使用标准 SQLite 语法，只允许 SELECT 语句
2. 规则必须来源于知识文档，不得凭空编造
3. 每条规则至少有一个 SQL 参考示例
4. 总数控制在 8-20 条规则
5. 输出必须是完整的 Markdown，不要有省略号或"略"