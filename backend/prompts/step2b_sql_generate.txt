你是银行数据工程师。请将以下业务规则翻译成可在 SQLite 中执行的 SQL 查询语句。

目标数据库说明：
- 数据库为 SQLite，表结构如下：
{{table_schema}}

待翻译的知识条目：
子场景：{{sub_scenario}}
阶段：{{step_phase}}
业务描述：{{knowledge_desc}}
知识引用（数据来源）：{{knowledge_ref}}
规则引用：{{rule_ref}}
期望输出：{{output}}

要求：
1. SQL 必须只使用 SELECT 语句，不允许 INSERT/UPDATE/DELETE/DROP。
2. 如果规则涉及多个条件，请用 AND/OR 表达。
3. 如果规则无法精确翻译为 SQL，请给出最接近的伪 SQL 并在注释中说明。
4. 输出 JSON：{"sql": "...", "tables": [...], "fields": [...], "confidence": "high|medium|low"}
